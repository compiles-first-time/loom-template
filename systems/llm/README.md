# EMBER systems atlas — machine pack (registry 95b296a15be1)

> Generated by `scripts/systems-map.sh render` from `systems/registry/*.md` and `systems/runbooks/*.md` (ADR-0065: the atlas; ADR-0066: runbooks, this pack, the edit hook). Do not edit; edit the registry. Read this file first; then **query, do not load** — the two JSONL files together are 593 KB (~152k tokens).

## What this is
A ledger of every system in the game (719 nodes in 16 domains, tiers 1 domain → 2 system → 3+ part) and every dependency between them (1189 edges), each edge carrying **how** it depends, **via** what, how **strong**, and **why**. It exists so that an agent about to change one thing can know what else moves, where, and why — before editing.

## Files
| File | One record per | Use it to |
|---|---|---|
| `nodes.jsonl` | system or signal | look a system up by id; see its paths, owner, phase, status, parts, and precomputed reach (`blast_hard`, `blast_all`, `upstream`) |
| `edges.jsonl` | influence edge `src → dst` | see who depends on what and why; `src`/`dst` is the direction a change travels, `from`/`to` is the row as written |
| `runbooks.jsonl` | change runbook | get the ordered procedure for a kind of change (add an item, a spell, a signal, a schema field …) |
| `summary.json` | — | counts, domains, load-bearing systems, the signal table, candidates awaiting the Director, edge vocabulary |

## How to query (prefer these over reading the files)
```
scripts/systems-map.sh which <path>          # which system owns this file?
scripts/systems-map.sh checklist <id>        # what to touch / check / run, in order
scripts/systems-map.sh runbook <rb_id>       # the procedure for a kind of change (runbooks: list)
scripts/systems-map.sh impact <id>           # PR-ready report: downstream + upstream + review checklist
scripts/systems-map.sh show <id>             # one system: parts, direct in/out edges
scripts/systems-map.sh find <text>           # search ids, names, summaries, paths
scripts/systems-map.sh audit-diff [--base r] # which systems this diff touches and what it did not
grep '"id":"<id>"' systems/llm/nodes.jsonl   # one record; grep '"domain":"combat"' for a domain
```
Add `--json` to any query for machine output.

## Reading a record
- **status** decides what you may do: `spec` = in GAME_INFRA_SPEC.md, build it · `implied` = the spec needs it but does not name it, build it and cite the section · `candidate` = the Director asked for it but the spec does not have it — **stop; a DIRECTOR decision and a spec PR come first (§14)** · `non-goal` = ruled out.
- **owner** is a spec §7.1 role with a write scope: orchestrator (anywhere, via PR) · content-smith (data/**, art/icons/**, changelog) · world-builder (scenes/**, art/**, data/biomes/**) · quest-writer (data/quests/**, data/dialogue/**, docs/lore/**) · test-pilot (tests/**, tools/testing/**, workflows) · director (decides).
- **phase** is when it lands (spec §13: 0 setup · 1 feel · 2 data spine · 3 survival loop · 4 multiplayer · 5 content factory). A hard dependency on a later-phase system is a design finding.
- **where** is the path the system lives at; `which <path>` inverts it. **spec** is the section that defines it.
- Edge **how** vocabulary: `listens` (A listens to EventBus signal B) · `emits` (A emits EventBus signal B (written from the emitter)) · `reads` (A reads B's data or state) · `references` (A's data references B's ids (a content-level coupling)) · `calls` (A calls B directly (R2: only inside one system subtree or a pure helper)) · `renders` (A (presentation) renders B's state — never mutates it (R5)) · `validates` (A (a gate or tool) validates B) · `persists` (A saves/loads B's state) · `configures` (A configures B) · `extends` (A is a specialization or variant of B) · `transports` (A (networking) carries B's state or commands) · `gated_by` (A is blocked until B approves or unlocks it).
- Edge **strength**: `hard` = breaks or must change when src changes · `soft` = should be reviewed.
- Signals are nodes named `sig_<signal>` under `event_bus`; `emits`/`listens` edges wire them. The registry's `sig_*` set is cross-checked against spec §5 (R-EB1).

## The change protocol (what an agent does with this)
1. `which <path>` (or read the hook's context line) → the system id.
2. `checklist <id>` → read §0 (stop conditions, owner, runbook) and §2 (must-check systems).
3. If a runbook applies, follow it step by step; each step names the system, the artifact and how to verify.
4. Make the change inside the owner's write scope. New signal or schema field ⇒ spec PR in the same change (§5/§6, §14).
5. Run the gates for the phase (§8), then `scripts/systems-map.sh validate` and `render --check`, then `bash scripts/doctor.sh`.
6. Added, removed or rewired a system? `add-node` / `add-edge` / `set-node` / `remove-*` — they validate and refuse to leave the ledger invalid — then `render`.
7. One line in `docs/changelog.md`; paste the checklist's hard rows into the PR.

## Do not
- hand-edit `systems/ATLAS.md`, `systems/atlas/`, `systems/explorer.html` or this directory (generated; the hook denies it);
- build a `candidate` system, add a signal without a §5 row, or add a dependency/addon/service without a spec PR (R10);
- load both JSONL files whole into context — grep by id or domain, or use the CLI.

## Load-bearing systems (largest blast radius)
- `data_schemas` — 412 systems downstream (Foundation)
- `architecture_rules` — 378 systems downstream (Foundation)
- `documentation` — 366 systems downstream (Operations)
- `art_pipeline` — 332 systems downstream (Presentation)
- `engine_platform` — 308 systems downstream (Foundation)
- `state_model` — 293 systems downstream (Foundation)
- `items` — 280 systems downstream (Economy)
- `time_and_tick` — 270 systems downstream (Foundation)

## Runbooks (18)
- `rb_add_biome` — Add a biome or zone · primary `terrain_biomes` · DIRECTOR decision involved
- `rb_add_building_piece` — Add a placeable building piece (station, storage, bed, wall, decoration) · primary `piece_defs` · DIRECTOR decision involved
- `rb_add_dialogue` — Add a dialogue tree (DialogueDef) · primary `dialogue_nodes_choices` · DIRECTOR decision involved
- `rb_add_enemy` — Add an enemy (EnemyDef + actor prefab) · primary `enemy_defs` · DIRECTOR decision involved
- `rb_add_item` — Add an item (ItemDef) · primary `items` · DIRECTOR decision involved
- `rb_add_loot_table` — Add a loot table (LootTableDef) · primary `loot_table_defs` · DIRECTOR decision involved
- `rb_add_material` — Add a crafting material (ore, wood, hide, herb …) · primary `materials_ingredients` · DIRECTOR decision involved
- `rb_add_npc` — Add a friendly NPC (NpcDef + actor prefab) · primary `npc_defs` · DIRECTOR decision involved
- `rb_add_quest` — Add a quest (QuestDef) · primary `quest_defs` · DIRECTOR decision involved
- `rb_add_recipe` — Add a crafting recipe (RecipeDef) · primary `recipe_defs` · DIRECTOR decision involved
- `rb_add_signal` — Add or change an EventBus signal (R-EB1) · primary `event_bus` · DIRECTOR decision involved
- `rb_add_spell` — Add a spell (SpellDef) · primary `spell_defs_content` · DIRECTOR decision involved
- `rb_add_status_effect` — Add a status effect (StatusEffectDef) · primary `effect_defs_content` · DIRECTOR decision involved
- `rb_add_system` — Add a new system or part to the atlas · primary `systems_atlas` · DIRECTOR decision involved
- `rb_add_verb` — Add a new verb (spell delivery, effect kind, AI behavior, objective type) · primary `casting` · DIRECTOR decision involved
- `rb_change_movement` — Change movement, camera, input translation or movement netcode (ADR-0068) · primary `locomotion` · DIRECTOR decision involved
- `rb_change_schema` — Add, rename or remove a field on a §6 schema · primary `data_schemas` · DIRECTOR decision involved
- `rb_retire_content` — Remove or deprecate a content entry (item, spell, effect, enemy, recipe, quest …) · primary `id_immutability_migrations` · DIRECTOR decision involved

